---
import BaseLayout from "@/layouts/BaseLayout.astro"
import { getAll } from "@/lib/kanji"


const level = "n2"
const all = getAll(level)

const title = `JLPT N2 Kanji Flashcards — Flip & Memorize`
const description = `Flip through all ${all.length} JLPT N2 kanji flashcards. Tap to reveal readings and meanings. Track your progress with Know/Again.`
---

<BaseLayout {title} {description}>
  <meta http-equiv="refresh" content={`0;url=/${level}/flashcards/${encodeURIComponent(all[0].kanji)}/`} />
</BaseLayout>
